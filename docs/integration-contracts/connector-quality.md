# Connector Quality Gate

Audit date: 2026-06-10

Every registered action connector is held to the same review dimensions before
it can be treated as production-ready. Equal depth does not mean every provider
has the same features; it means every connector explicitly documents the same
questions: validation, safe error handling, pagination or async status,
rate-limit/budget behavior, provider documentation, auth shape, audit coverage,
and remaining gaps.

Agents are the primary users of these contracts. The agent chooses strategy,
creates run plans, and passes explicit action inputs. StackOS connectors only
validate the input, resolve daemon-held credentials, call the provider operation,
return safe structured output, and write audit records.

## Minimum Standard

- **Validation**: action schema plus connector validation reject malformed
  payloads, unsafe generic goals, and secret-looking input.
- **Errors**: provider failures are normalized into safe responses and action
  call audit records without leaking authorization headers, tokens, passwords,
  or raw credential payloads.
- **Pagination/status**: list, report, crawl, async, and batch operations expose
  bounded cursors/status outputs or are documented as submit-only/deferred.
- **Rate limits/budget**: retries, backoff, provider units, credits, quota, or
  explicit non-budget semantics are visible in metadata and docs.
- **Provider docs**: official docs for auth, operation shape, pagination,
  errors, and rate limits are linked from the contract ledger.
- **Audit**: executable actions have mocked or local tests that prove grant
  enforcement, credential resolution, redaction, and action-call persistence.

## Registered Connector Matrix

| Connector | Executable action surface | Contract docs | Validation | Errors | Pagination/status | Rate limits/budget | Current signoff |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ahrefs` | `seo.competitor.keywords`, `seo.backlink.research` | [`current-connectors.md`](current-connectors.md) | Provider-specific target/mode/input validation, documented backlink mode enum, and plan-aware row caps from the free subscription-info endpoint. | Safe action-call failures; API-unit headers are captured in metadata for successful row calls. | First-page/list calls only; future backlinks/keyword pagination must be explicit. | Monetary budget enforcement disabled until API-unit accounting is modeled. | Executable, narrow, with tracked unit-budget gap. |
| `apollo` | People/org search and enrichment actions | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | Search/enrichment schemas bound to Apollo endpoint concepts; broad provider bags still need metadata tightening. | Connector must normalize 401/403/422/429 and credit failures. | Page/per_page contract required; stop before provider caps. | Credit-consuming endpoints require credit budget metadata. | Executable first pass; needs pagination/rate-limit tests before expansion. |
| `branding` | `branding.evidence.capture`, `branding.evidence.sanitize-mark` | [`current-connectors.md`](current-connectors.md) | Internal evidence payloads are bounded by the branding resource schema; capture is raw-only, and clearance requires a separate sanitize-mark verdict with reviewer, reason, and decision ref. | Repository validation failures return structured issues; no credentials or provider payloads exist for this connector. | Not paginated; actions upsert or update one evidence record at a time. | Internal resource actions have no provider quota or monetary budget. | Executable internal connector with manifest, repository, MCP visibility, and readiness coverage. |
| `clay` | `gtm.clay.table.webhook.submit` | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | Static table webhook payload validation; no generic Clay API action. | Webhook delivery errors must be normalized and redacted. | Async workflow result ingestion is separate/deferred. | Project-specific table/workflow throttles must be documented per connection. | Executable webhook submit only; enterprise API deferred. |
| `dataforseo` | Keyword volume, SERP, PAA | [`current-connectors.md`](current-connectors.md) | Keyword/depth caps validated against exposed action schemas. | Provider task errors and cost rows are normalized. | Current live endpoints are bounded; Labs pagination needs explicit actions before exposure. | `tasks[].cost` reconciles post-call cost; QPS honors documented limits. | Executable, narrow, with Labs gaps tracked. |
| `firecrawl` | Scrape, crawl submit, map | [`current-connectors.md`](current-connectors.md) | URL and option validation exists; format object support is intentionally not exposed yet. | Base integration retries/normalizes provider errors. | Crawl is submit-only until a crawl-status/pagination action exists. | Budget estimates exist for exposed calls; rate limiting uses the shared base. | Executable; crawl-status action required before templates consume crawl results. |
| `ghost` | Admin post create | [`current-connectors.md`](current-connectors.md) | Post payload and API version are scoped to create-post HTML source. | JWT/auth/post errors are normalized and redacted. | No list/update/image pagination actions exposed. | No monetary budget; provider throttling remains provider-error metadata. | Executable create-post only. |
| `google-ads` | Customer, budget, campaign, ad group, asset, ad, report, conversion actions | [`media-buying.md`](media-buying.md) | Provider-native schemas for first action set; more enums/mutate constraints still need tightening. | Google Ads error details must stay sanitized and include request ids where present. | Reporting/search pagination and mutate partial failures need explicit tests. | Quotas and conversion/mutate costs are documented but need richer metadata. | Executable first pass; high priority for deeper mocked provider tests. |
| `google-analytics` | GA4 account summaries, metadata, standard report, realtime report | [`current-connectors.md`](current-connectors.md) | Property refs resolve from credential config; report bodies reject nested `property`, require metrics/date ranges where documented, and bound page size/limit inputs. Public pagination input is `page_cursor`, mapped to Google `pageToken`. | OAuth and Data/Admin API failures flow through safe provider errors without token output. | Account summaries expose redaction-safe `next_page_cursor`; standard reports expose Google `rowCount` with caller-managed offset/limit; realtime reports are bounded single calls. | Monetary budget disabled; GA4 quota cost depends on request complexity, and returned `propertyQuota` is exposed as redaction-safe `quota` keys such as `units_per_day`. | Executable read-only SEO measurement connector; write/admin and Measurement Protocol deferred. |
| `google-workspace` | Gmail send, Calendar event create | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | Mail/event payloads must use explicit recipients/body/timezone inputs. | Gmail/Calendar errors and quota responses must be normalized. | Send/create are non-list operations; future reads need cursors. | Gmail unit costs and Calendar quotas documented; metadata should include quota diagnostics. | Executable first pass; send risk requires approval gates. |
| `google-search-console` | Sites, search analytics, sitemaps, URL inspection | [`current-connectors.md`](current-connectors.md) | Site and URL inputs are explicit; URL-prefix properties must include the trailing slash, row limits cap at 25,000, and URL inspection must stay under the property when syntactically provable. | OAuth and Search Console errors flow through safe provider errors without token output. | Search Analytics uses caller-managed `start_row`/`row_limit`; sites, sitemaps, and URL inspection are single-call reads. | Monetary budget disabled; Search Console and URL Inspection quotas remain provider-enforced. | Executable read-only SEO measurement connector; legacy `gsc*` surfaces remain forbidden. |
| `google-tag-manager` | Account, container, snippet, workspace, tag, and trigger inventory | [`current-connectors.md`](current-connectors.md) | Account/container/workspace refs resolve from safe credential config and build only API-relative paths; public pagination input is `page_cursor`, mapped to Google `pageToken`. | OAuth and Tag Manager API failures flow through safe provider errors without token output. | List actions expose redaction-safe `next_page_cursor`; no auto-pagination because GTM has low QPS. | Monetary budget disabled; wrapper QPS defaults conservatively for GTM quota. | Executable read-only SEO measurement connector; edit/publish/permission scopes deferred. |
| `http` | Project-local custom static HTTP actions | [`current-connectors.md`](current-connectors.md), [`action-executor.md`](../action-executor.md) | Static method/URL/auth/request mode validation; secret-looking static headers rejected. | HTTP status errors are safe but need richer problem-detail bodies. | Connector does not page; project-local actions must model cursors in their schema. | No shared retry/rate-limit policy yet; project-local policy must be explicit. | Executable escape hatch with strict review requirements. |
| `hubspot` | CRM batch upserts, notes/tasks, deals search/list | [`gtm-crm.md`](gtm-crm.md) | Provider-native object schemas; field-mapping metadata still needs tightening. | HubSpot multi-status and correlation ids must be normalized. | Search/list cursors and 10k caps must be bounded in inputs/outputs. | OAuth rate-limit headers and daily limits documented; metadata needs coverage. | Executable first pass; needs cursor/error tests. |
| `jina` | Reader fetch | [`current-connectors.md`](current-connectors.md) | Absolute HTTP(S) target validation is required before further expansion. | Provider/text fetch errors are safe; long response behavior needs output guidance. | No pagination; large output should move toward artifact/resource persistence. | Optional credential improves quota; no StackOS budget gate. | Executable narrow read. |
| `meta-ads` | Account, campaign, ad set, ad, creative, insights, budget, conversion actions | [`media-buying.md`](media-buying.md) | Provider hierarchy modeled explicitly; remaining enums and creative variants need tightening. | Graph API errors must preserve safe `fbtrace_id` and redact tokens. | Insights pagination/breakdowns require explicit cursors and caps. | Graph rate limits and spend-bearing mutations require approval/budget metadata. | Executable first pass; high priority for mocked provider depth. |
| `microsoft-365` | Graph mail send, calendar event create | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | Payloads scoped to Graph send/create operations and safe mailbox/calendar refs. | Graph `request-id`, throttling, and async `202` semantics must be normalized. | Send/create are non-list operations; future reads need Graph paging. | Graph `Retry-After` and throttling limits documented; metadata needs coverage. | Executable first pass; send/create need approval gates. |
| `mock-provider` | `utils.mock.echo` | [`../integration-testing.md`](../integration-testing.md) | Local scenarios cover success, partial success, provider errors, auth errors, rate limits, and timeout. | Redaction/audit failure path is tested without live credentials. | Not paginated; scenarios prove status handling. | Local cost_cents input proves budget/audit plumbing without vendor accounts. | Local proof connector for REST, CLI, and MCP. |
| `openai-images` | Image generation and reference-image edit | [`current-connectors.md`](current-connectors.md) | GPT Image model/size/quality/formats, prompt length, edit ref count, input format, 50 MB input image cap, and `input_fidelity` model support are validated against supported profiles. | API errors are normalized; raw base64 is stripped from persisted output and repository-backed executions register generic image artifacts. | Not paginated. | Model/size/quality estimates are encoded; pricing requires scheduled doc audit. | Executable with cost estimates. |
| `xai-imagine` | Grok image generation, image edit, and async video generation | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | StackOS v1 model ids, aspect/resolution enums, batch counts, reference-image counts, duration caps, poll bounds, and mutually exclusive video modes are validated before provider calls; undocumented preview video ids are not exposed. | API and async job failures are normalized; raw base64, temporary URLs, and credentials are not returned after persistence. | Image actions are single-call; video generation is submit/poll/download with terminal `done`/`failed`/`expired` handling. | Official Imagine pricing units are encoded for pre-call budget checks; successful action-call costs reconcile from `usage.cost_in_usd_ticks` when present, with budget top-up if actual exceeds estimate. | Executable first pass with generated-assets persistence, generic media artifact registration, and MCP grant coverage. |
| `reve` | Image create, edit, and remix | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Official rendered-doc limits are enforced: 2560-character prompt/instruction cap, aspect/version enums, 1-15 test-time scaling, one edit image, 1-6 remix images, and 32 million total remix pixels. | API errors flow through the shared wrapper; raw base64 images and reference-image bytes are not returned after persistence. | Single synchronous image calls only. | Official credit estimates are encoded and successful costs reconcile from `credits_used`; `auth.test` is format-only to avoid hidden billable probes. | Executable first pass with generated-assets persistence, generic image artifact registration, and MCP grant coverage; live credential smoke remains operator evidence. |
| `google-gemini-image` | Gemini Nano Banana image generation and editing | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Model ids, model-specific aspect ratios, Gemini 3 image-size enums, input-ref counts, generated-assets ref paths, input formats, and the inline 20 MB request envelope are validated before provider calls. | API errors flow through the shared wrapper; raw inline base64 images and reference-image bytes are not returned after persistence. Usage metadata is normalized to redaction-safe keys. | Single synchronous `generateContent` image calls only; Files API, chat state, grounding tools, and video input are deferred. | Official output-image prices are encoded for pre-call checks, plus Gemini 3 Pro Image input-image equivalent where applicable; tokenized text/image input charges remain provider-invoiced. | Executable first pass with generated-assets persistence, generic image artifact registration, and MCP grant coverage; live credential smoke remains operator evidence. |
| `google-veo` | Gemini API Veo video generation | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Veo model ids, modes, duration bounds, aspect ratios, optional resolution/person/seed controls, generated-assets image refs, and polling bounds are validated before provider calls. | Long-running operation errors and malformed media responses surface as safe integration failures; provider video URIs and API keys are not returned after persistence. | Async submit/poll/download flow using `predictLongRunning` and `operations/{id}`; text-to-video, image-to-video, and first/last frame modes are exposed. | Budget estimate is zero until Veo model/resolution/second pricing is mapped into pre-call budget logic; budget kind is present for future enforcement. | Executable first pass with mocked wrapper/action tests and generated-assets video artifact registration; live credential smoke remains operator evidence. |
| `ideogram` | Ideogram 4.0 image generate and remix | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Official 23 resolution enums, rendering-speed enum excluding currently failing `FLASH`, one remix image, image-weight range, input formats, 10 MB (10,000,000 byte) upload cap, and generated-assets path containment are validated before provider calls. | API errors flow through the shared wrapper; temporary provider URLs are downloaded, stripped from outputs/audit, and replaced with generated-assets refs. | Single synchronous multipart image calls only; helper, background, upscale, legacy edit, and custom-model endpoints are deferred. | Official per-output prices are encoded for pre-call checks by rendering speed and reconciled against returned output count; `auth.test` is format-only to avoid hidden billable probes. | Executable first pass with generated-assets persistence, generic image artifact registration, and MCP grant coverage; live credential smoke remains operator evidence. |
| `byteplus-seedream` | BytePlus Seedream image generation and reference-image editing | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Official priced model ids, regions, model-specific abstract/custom size limits, reference-image count, input formats/dimensions, per-image byte caps, sequential-generation controls, combined reference-plus-output cap, watermark type, and lite-only output format are validated before provider calls. | API errors flow through the shared wrapper; temporary provider URLs and raw base64 outputs are persisted to generated assets and stripped from returned output/audit. | Single synchronous ModelArk image calls only; streaming partial images and Seededit-specialized controls are deferred. | Official per-successful-output prices are encoded for pre-call checks, reserving the requested generated image count for sequential calls; action-call cost records reconcile from `usage.generated_images` or persisted output count, while budget spend remains conservative precharge/top-up state. | Executable first pass with generated-assets persistence, generic image artifact registration, and MCP grant coverage; live credential smoke remains operator evidence. |
| `byteplus-seedance` | BytePlus Seedance video generation | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Seedance model ids, regions, resolution/ratio/duration bounds, reference image/video/audio counts, local image refs, flags, priority, and polling bounds are validated before provider calls. | Async task failures and malformed outputs are normalized safely; temporary output URLs and local input bytes are not returned after persistence. | Async create/retrieve/download flow through ModelArk content generation tasks; text, image, first/last frame, and reference media modes are exposed. | Pricing remains provider-usage based and is not pre-estimated; budget kind `byteplus-ark` is present for later usage-token accounting. | Executable first pass with mocked wrapper/action tests and generated-assets video artifact registration; live credential smoke remains operator evidence. |
| `alibaba-wan` | Alibaba Model Studio Wan video generation | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Region, mode, duration, resolution/aspect, URL-only media inputs, prompt flags, seed, and polling controls are validated before provider calls. | DashScope task failures and missing `video_url` responses are normalized; temporary URLs and API keys are stripped from outputs/audit after persistence. | Async submit/poll/download flow; text-to-video, image URL, first/last frame URL, and video-continuation URL modes are exposed. | Pricing is not pre-estimated yet by operator direction; budget kind `alibaba-wan` is present for later per-second modeling. | Executable first pass with mocked wrapper/action tests and generated-assets video artifact registration; live credential smoke remains operator evidence. |
| `kling-video` | Kling text-to-video and image-to-video | [`current-connectors.md`](current-connectors.md), [`media-generation.md`](media-generation.md) | Access-key credential shape, model ids, quality mode, duration, text aspect ratio, sound, cfg_scale, local image refs, and polling controls are validated before provider calls. | Kling service codes, failed task statuses, malformed videos, credentials, JWTs, and provider URLs stay out of returned output after persistence. | Async task create/query/download flow for text-to-video, image-to-video, and first/last frame modes. | Pricing is not pre-estimated yet; budget kind `kling` is present for later credit/second modeling. | Executable first pass with mocked wrapper/action tests and generated-assets video artifact registration; live credential smoke remains operator evidence. |
| `outreach` | Sequence state create | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | JSON:API sequence-state payload required; generic sequence add avoided. | JSON:API errors must preserve safe title/detail/status fields. | Create is non-list; future Outreach reads need cursor or offset contracts. | OAuth/rate limits documented; throttle policy refs needed for sends. | Executable first pass; outbound risk requires approval gates. |
| `pipedrive` | Deals list/search | [`gtm-crm.md`](gtm-crm.md) | Deal filters and selected fields should remain bounded and provider-native. | Pipedrive error body should be normalized safely. | Cursor/list pagination must expose next cursor and caps. | Cost/rate-limit behavior documented; metadata needs coverage. | Executable read first pass. |
| `reddit` | Subreddit search/top posts | [`current-connectors.md`](current-connectors.md) | Sort/time_filter enums and pagination inputs still need tightening. | OAuth/listing errors are normalized without token exposure. | First slice only today; `after`/`before` pagination should be added before expansion. | OAuth app quota/status documented; no StackOS budget gate. | Executable narrow read. |
| `salesforce` | External-ID upserts, task create, opportunity query | [`gtm-crm.md`](gtm-crm.md) | sObject/external-ID/update-only contracts required; no arbitrary free-form SOQL. | Salesforce error arrays and request-limit failures must be normalized. | Query `nextRecordsUrl` requires bounded continuation before large reads. | API limits and `Sforce-Limit-Info` should be captured. | Executable first pass; needs strict query and limit tests. |
| `salesloft` | Cadence membership create | [`gtm-prospecting-outbound.md`](gtm-prospecting-outbound.md) | Cadence membership payload must use visible person/cadence refs. | 422 and cadence ownership errors must be safe and actionable. | Create is non-list; future reads need page/per_page contracts. | Team-level cost/rate limits documented; metadata needs coverage. | Executable first pass; outbound risk requires approval gates. |
| `serper` | Google Search/SERP search | [`current-connectors.md`](current-connectors.md) | Query, result count, page, country, language, and Google filter inputs are bounded before provider calls. | Provider errors flow through the shared integration wrapper and action-call failure path without exposing the key. | Single search response only; no pagination beyond explicit page input. | Provider credit usage is surfaced when present but not pre-budgeted yet. | Executable narrow SEO search action. |
| `shopify` | 58 curated Shopify Admin GraphQL/ShopifyQL actions | [`current-connectors.md`](current-connectors.md), [`shopify.md`](shopify.md), [`shopify-action-signoff.md`](shopify-action-signoff.md) | Static manifest schemas plus connector variable builders restrict agents to curated action inputs; raw GraphQL and arbitrary ShopifyQL are never accepted. | HTTP, GraphQL `errors`, and mutation `userErrors` are normalized without exposing Admin API tokens. | Most actions are bounded first-page calls with cursor inputs; `inventory_risk_report` caps internal pagination and reports truncation. | Shopify GraphQL cost metadata is preserved, but no monetary budget is enforced because Shopify cost points are not dollars. | Executable with mocked auth, catalog, and action tests plus July 8, 2026 per-action docs signoff; live store smoke remains operator/release evidence. |
| `sitemap` | Public sitemap fetch | [`current-connectors.md`](current-connectors.md) | XML URL/index caps, recursion, and max_entries are bounded. | Partial failures are preserved instead of collapsing the whole fetch. | Sitemap recursion is bounded; no cursor paging. | No provider quota; StackOS byte/entry caps are documented safety limits. | Executable mature utility. |
| `slack-bot` | Identity, message send, conversation open/info/list, member list, and signed HTTP ingress normalization | [`communications.md`](communications.md) | Project-scoped `communication-profile` can bind outbound interaction state; payload validation rejects malformed Block Kit buttons, secret-like button values, bad refs, unsupported conversation types, and unbounded list inputs. | Provider errors are normalized and Slack token-shaped strings are redacted; ingress rejects invalid signatures before storing resources. | Conversation list/member calls expose bounded limits and Slack cursors; HTTP ingress is idempotent by profile-scoped event/request keys. Socket Mode and live history reads are deferred. | Slack API has no StackOS monetary budget; connector records zero-cost actions and preserves safe `Retry-After`/cursor metadata where present. | Executable with mocked Web API actions, signed ingress, Block Kit lifecycle, no-secret, auth-test, resource, and whitelist coverage; Socket Mode deferred. |
| `smtp` | `communications.smtp.email.send` | [`communications.md`](communications.md) | Recipients, subject, body choice, headers, reply/from refs, and source request refs are validated before SMTP calls. | SMTP auth/transport/provider rejection paths return safe accepted/rejected metadata and redact password material. | Send is non-list and non-status; SMTP acceptance is explicitly not delivery, read, open, click, or reply state. | No StackOS monetary budget; timeout/TLS mode are credential config and provider throttling remains provider-error metadata. | Executable with mocked TLS, rejection, no-secret, resource, and auth-test coverage. |
| `taboola` | Account, campaign, item, report, conversion actions | [`media-buying.md`](media-buying.md) | Account-scoped Backstage schemas; remaining enums/status rules need tightening. | OAuth/API errors must include safe provider diagnostics. | Reports and dictionaries need pagination/cursor caps before broader use. | OAuth token/rate-limit behavior documented; budget metadata needed for spend writes. | Executable first pass; high priority for mocked provider depth. |
| `telegram-bot` | Identity, message/photo send, callback answer, diagnostic update inspection, webhook set/delete/info | [`communications.md`](communications.md) | Project-scoped `communication-profile` required for sends/webhooks; payload validation rejects unsafe callback data, bad media source choices, profile/credential mismatches, wrong webhook paths/hosts, and malformed origin-bound replies. | Provider errors are normalized and Telegram bot-token URLs are redacted. | `updates.poll` is bounded diagnostic/bootstrap access only; webhook ingress owns normal message/callback normalization and request creation. | Telegram API has no StackOS monetary budget; connector records zero-cost actions and documents public webhook host allowlisting. | Executable first communications slice with mocked Bot API, ingress, redaction, profile, callback lifecycle, and origin-binding tests. |
| `trackbooth` | Catalog sync/search/describe plus generated Agent API actions from live export | [`current-connectors.md`](current-connectors.md), [`trackbooth.md`](trackbooth.md) | Generated actions preserve catalog schemas; sync validates source URL, API-key auth, body/query/path shapes, and blocked credential operations. | Provider failures surface validation/auth/permission details without exposing API keys or acting-account secrets. | Catalog sync uses bulk export; generated list/report actions rely on catalog-defined schemas and file-backed outputs for large responses. | API URL defaults to production and supports localhost/remote config; request budgeting is context-level follow-up work. | Executable fixed connector with generated inventory sync, stale-action hiding, schema preservation, and MCP context support. |
| `imap` | Mailbox list, bounded UID search, selected message fetch, and mark seen/unseen | [`communications.md`](communications.md) | Mailbox refs, UID-only operations, criteria, fields, body caps, and mark lifecycle inputs are validated before IMAP calls. | IMAP login/select/search/fetch/store failures are normalized through action-call failures without exposing passwords. | UID search returns bounded UID lists plus UIDVALIDITY cursor resources; fetch stores selected message metadata, and mark operations store status events. | No StackOS monetary budget; provider throttling is surfaced as provider error metadata, and background polling is not built into the connector. | Executable with mocked SSL/starttls-ready auth, UID search/fetch/store, no-secret, resource, and credential-test coverage. |
| `wordpress` | REST post create | [`current-connectors.md`](current-connectors.md) | Post payload and application-password auth shape documented; capability checks need clearer status. | REST errors normalized; role/capability diagnostics should improve. | No list/update/media pagination actions exposed. | No monetary budget; HTTPS/application password setup is required. | Executable create-post only. |

## Release Rule

Before adding or expanding a connector, update this matrix, the relevant
provider-contract ledger, the plugin action manifest, mocked connector tests,
grant tests, and the before-commit/release command set in
[`../release-signoff.md`](../release-signoff.md).
