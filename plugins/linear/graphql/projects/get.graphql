query LinearProjectGet($id: String!) {
  project(id: $id) {
    id
    name
    slugId
    description
    status {
      id
      name
      type
    }
    priority
    startDate
    targetDate
    url
    archivedAt
  }
}
