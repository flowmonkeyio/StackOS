<script setup lang="ts">
const { workflows: items } = useLibraryCatalog()

useLibrarySeo({ title: `AI workflow automation library — ${items.length} complete workflows | StackOS`, description: `Explore ${items.length} workflow contracts with visible stages, state, tool boundaries, outputs, recovery, and verification across engineering, content, marketing, sales, support, SEO, communications, and paid media.` })
useSchemaOrg([defineWebPage({ '@type': 'CollectionPage', name: 'StackOS Workflow Library' }), defineBreadcrumb({ itemListElement: [{ name: 'Home', item: '/' }, { name: 'Library', item: '/library' }, { name: 'Workflows', item: '/library/workflows' }] })])
useHead({ script: [{ key: 'workflow-library-faq', type: 'application/ld+json', innerHTML: JSON.stringify({ '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: [{ '@type': 'Question', name: 'What is AI workflow automation?', acceptedAnswer: { '@type': 'Answer', text: 'AI workflow automation gives an AI system a reusable execution contract from a request to a verified result. The contract defines state, dependencies, scoped tools and actions, expected outputs, recovery, and completion criteria.' } }] }) }] })
</script>

<template><LibraryFrame><CatalogIndex kind="workflows" title="Complete workflows for" accent="real work." description="A workflow gives AI a reusable execution contract from request to verified result. Browse each StackOS workflow and inspect the stages, state, tool boundaries, outputs, and recovery paths inside." answer-title="What is AI workflow automation?" answer="AI workflow automation combines a reusable workflow contract with a concrete run plan for the current request. The workflow defines the operating boundary; the run records state, dependencies, scoped tools, outputs, recovery, and verification." :answer-points="[{ label: 'Workflow', text: 'The reusable contract and rules for a type of work.' }, { label: 'Run plan', text: 'The concrete steps, state, grants, and outputs for this request.' }, { label: 'Verification', text: 'Acceptance criteria and receipts show whether the result is complete.' }]" answer-link="/library/articles/what-is-an-agentic-workflow" answer-link-label="Learn how agentic workflows work" :items="items" /></LibraryFrame></template>
